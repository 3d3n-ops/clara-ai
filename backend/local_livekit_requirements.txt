# Local LiveKit Agent Requirements
# Minimal dependencies for running the local LiveKit agent script

# Core dependencies
asyncio
pathlib
typing
datetime
json
re
time
sys
os

# Optional: For enhanced functionality
# aiofiles>=23.0.0      # For async file operations
# rich>=13.0.0          # For better terminal output
# click>=8.0.0          # For command line interface
# colorama>=0.4.6       # For colored terminal output (Windows compatibility)

# Note: These are all built-in Python modules, so no pip install needed
# The script will work with Python 3.7+ out of the box 